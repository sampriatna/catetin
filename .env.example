# .env.local — JANGAN commit ke git

# ── Supabase ────────────────────────────────────────────────
# Dari: Supabase Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Service role key — hanya untuk server routes (api/pair)
# Dari: Supabase Dashboard → Project Settings → API → service_role
# JANGAN pakai prefix NEXT_PUBLIC_ — ini tidak boleh ke browser
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ── Claude AI ───────────────────────────────────────────────
# Dari: console.anthropic.com → API Keys
# JANGAN pakai prefix NEXT_PUBLIC_ — ini tidak boleh ke browser
ANTHROPIC_API_KEY=sk-ant-...

# ── App URL ─────────────────────────────────────────────────
# URL publik NF3 — dipakai link reset password, undangan staf, pair web
# Production — domain utama Vercel (pair, undangan, reset password):
NEXT_PUBLIC_APP_URL=https://catatin.nusafishing.com
# Dev lokal (hanya jika tes tanpa domain):
# NEXT_PUBLIC_APP_URL=http://localhost:3001

# Bisnis utama — semua staf harus pakai ini (dompet & transaksi terpusat)
NEXT_PUBLIC_CANONICAL_BUSINESS_ID=e23ed572-234c-4995-acad-fa6bff7c58d2

# Opsional dev: izinkan daftar tanpa link undangan (jangan set di production)
# NEXT_PUBLIC_ALLOW_OPEN_SIGNUP=1

# ── Email (opsional, untuk kirim undangan via email) ────────
# Daftar di resend.com — free tier 3000 email/bulan
# RESEND_API_KEY=re_...
